# Claude Flow V3 - npm Configuration
#
# Note: We accept 7 build-time only vulnerabilities in the dependency chain.
# See SECURITY.md for detailed threat analysis and justification.
#
# The vulnerable packages (tar, node-gyp) are ONLY used during native module
# compilation and have NO runtime impact on the CLI.
#
# This is documented and accepted per security policy.
