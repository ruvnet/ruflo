# Exposed-training inherited ranker result

Decision: accept the engineering scaffold only. A child process can derive
versioned diagnosis, patch-family and test-selection state, then independently
produce and score two successor descriptors for each declared arm without
post-spawn researcher input. It cannot generate patch bytes or execute repository
candidates, so the executable-proposer blocker remains and RSI is unproven.

Source `5d4c60f3906a4a70973e4c11f33db95a31def172` at tree
`249b1a48bbd7f63ec16b85219142cdd0d65ea74c` binds plan semantic hash
`d9ff24763a9398763e79bde5741b8e5ec092b5da642f28c827bdd78008b002a0`
and raw SHA-256
`55292e89055f5f5e81478e442c38ac7b451a1540f9d1125eb4a12ddd9b4683b9`.
The reservation is retained at commit
`ef059bf837703b62c069f4e8a6ff331c2d56638d`.

Three source-bound acceptance runs passed 45/45 tests. They observed 15 of 15
child-process starts, computed 120 descriptor scores and took 400, 435 and 434 ms:
1269 ms summed outer wall time. They consumed zero repair candidate evaluations,
isolated process starts, native field calls, repair wall time, improver model calls
or provider spend. Outer Codex model cost and total acquisition, evaluation and
engineering dollar costs remain unknown.

A read-only review first found shuffled-state leakage, an invented predecessor,
weak descriptor lineage, misleading process accounting, raw-plan identity gaps and
unbounded plan reads. The corrected code uses all three learned state dimensions,
shuffles each independently, binds generation-zero `previous` honestly to its
degenerate `frozen` state, binds descriptors to arm/plan/state/lineage, separates
in-process from observed child starts, and reads a raw-hash-pinned regular plan
through a bounded no-follow descriptor. Final review found no remaining critical
or high issue in this scaffold.

All scores use the exposed p-limit failure and published fix. Any apparent arm
advantage is circular training fit, not a fresh-task result. No empirical
hypothesis was admitted, no improvement-capacity comparison was executed, and the
unapproved proposal for 36 evaluations, 216 isolated starts and 1080000 summed
process milliseconds is unchanged.

Largest uncertainty: whether inherited failure-analysis state helps descendants
produce better successors on fresh tasks at equal cost. Next acceptance requires
an executable patch proposal evaluated on a compatible clean isolated runner,
after fresh development tasks are frozen and the repair resource envelope is
explicitly approved.

## Acceptance

```bash
node --test v3/@claude-flow/cli/scripts/rsi/repair/inherited-improver.test.mjs
```

Expected: fifteen passes with five observed child results; repair execution,
fresh-task outcome reading, improvement-capacity evidence and RSI acceptance remain
false.
