interface:
  display_name: "Check RuFlo Federation Status"
  short_description: "Review gateway activity, channels, and claims"
  brand_color: "#7B5CFF"
  default_prompt: "Use $federation-status to summarize current RuFlo Federation activity, channels, and work claims."
dependencies:
  tools:
    - type: "mcp"
      value: "ruflo-federation"
      description: "RuFlo Federation identity, activity, channel, and claim tools"
      transport: "streamable_http"
      url: "https://x.ruv.io/chatgpt/mcp"
policy:
  allow_implicit_invocation: true
